'aaa'

count max-continuous occurrence 

iterate over the string 

if current Letter is same as previous letter then keep incrementing the count variable
also track the currentLetter 

if current Letter is not same as previous letter then store count in some (highest count variable)
repeat line 7, 8, 10
// testMaxSequence('a', 'a,1');
  // testMaxSequence('aaa', 'a,3');
  // testMaxSequence('aaaa', 'a,4');
  // testMaxSequence('aabbaaa', 'a,3');
  // testMaxSequence('abba', 'b,2');